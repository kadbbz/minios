# Bootstrap Skills

Bootstrap skills are built-in session-critical skills.

They differ from bundled skills:

- Bootstrap skills load for every agent session automatically.
- Bundled skills are preinstalled in the image but enabled per agent as needed.

Current bootstrap skills:

- `file-transfer-bootstrap`
